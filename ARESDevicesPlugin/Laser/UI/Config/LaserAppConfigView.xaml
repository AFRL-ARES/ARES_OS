<UserControl x:Class="AresCNTDevicesPlugin.Laser.UI.Config.LaserAppConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Mvvm="http://prismlibrary.com/"
             xmlns:Serial1="clr-namespace:ARESCore.DeviceSupport.Serial;assembly=ARESCore"
             Mvvm:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
  <Border BorderThickness="2" BorderBrush="{DynamicResource AccentColorBrush}" Margin="5">
    <Grid>
   <Grid.ColumnDefinitions>
     <ColumnDefinition/>
     <ColumnDefinition Width="Auto"/>
   </Grid.ColumnDefinitions>
      <StackPanel Orientation="Vertical">
        <TextBlock Text="Laser Configuration" FontSize="18"  HorizontalAlignment="Center"/>
        <Serial1:Rs232Config HorizontalAlignment="Stretch" DataContext="{Binding AppConfig}" />
      </StackPanel>
      <Button Grid.Column="1" Margin="5" Style="{StaticResource AccentedSquareButtonStyle}" Content="Commit" 
              Command="{Binding CommitValuesCommand}"/>
    
  </Grid>
  </Border>
</UserControl>