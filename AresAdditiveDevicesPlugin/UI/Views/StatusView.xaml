<UserControl x:Class="AresAdditiveDevicesPlugin.UI.Views.StatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AresAdditiveDevicesPlugin.UI.Views"
             xmlns:Mvvm="http://prismlibrary.com/"
             xmlns:vms="clr-namespace:AresAdditiveDevicesPlugin.UI.Vms"
             xmlns:views="clr-namespace:AresAdditiveDevicesPlugin.PythonStageController.UI.Views"
             Mvvm:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vms:StatusViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="8*" />
        </Grid.RowDefinitions>
            <views:PythonStageControllerView />
        <Viewbox Grid.Row="1">
        <views:PythonStageControllerPositionView HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" />
        </Viewbox>
        <views:ToolpathConfiguratorView Grid.Row="2" />
    </Grid>
</UserControl>
